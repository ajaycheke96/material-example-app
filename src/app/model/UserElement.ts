export class UserElement {
    constructor(
        id: number,
        username: String,
        email: String,
        roles: [],
        createdAt: String,
        enabled: number,
        rememberToken: String,
        status: String,
        updatedAt: String,
        uuid: String
    ) { }
}