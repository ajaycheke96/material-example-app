export class AcademicSessionElement {
    constructor(
        id: number,
        createdAt: String,
        description: String,
        endDate: String,
        isDefault: number,
        name: String,
        options: String,
        startDate: String,
        updatedAt: String
    ) { }
}